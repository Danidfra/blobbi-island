/**
 * Re-export of the arcade-wide query provider, kept at this path so the dance
 * tests' established import site survives the provider's move to
 * `../test-providers` (shared with the hockey and pool machine tests).
 */
export { QueryProviders } from '../test-providers';
